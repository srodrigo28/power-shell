## Dica de Segurança
> * Para rodar scripts .ps1 no seu Windows pela primeira vez, você pode precisar liberar a permissão de execução (como administrador):
```
Set-ExecutionPolicy RemoteSigned -Scope CurrentUsers
```

git config user.name
git config user.email